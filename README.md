# Top Ramen

Monorepo for client facing app. It includes codebase for Web, Android and IOS.

## Installation

```
#Setup steps
yarn install
cd commons && yarn && cd ..
cd native && yarn && cd ..
cd web && yarn && cd ..

// Run mock server
yarn run mock
```